<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- Header -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Icon/Logo di kiri -->
        <ImageView
            android:id="@+id/imgLogo"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:padding="8dp"
            android:background="#82D7FF"
            android:src="@drawable/category"
            android:contentDescription="Logo" />

        <!-- Teks Kategori di samping logo -->
        <TextView
            android:id="@+id/tvKategori"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/imgLogo"
            android:layout_centerVertical="true"
            android:layout_marginStart="12dp"
            android:text="Kategori"
            android:textSize="18sp"
            android:textColor="#2C3E50" />

        <!-- Status Aktif di kanan -->
        <LinearLayout
            android:id="@+id/layoutAktif"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:visibility="visible">

            <ImageView
                android:id="@+id/imgStatusAktif"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ceklist"
                android:contentDescription="Status Aktif" />

            <TextView
                android:id="@+id/tvAktif"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="Aktif"
                android:textSize="12sp"
                android:textColor="#090909" />

        </LinearLayout>

    </RelativeLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#E0E0E0" />

</LinearLayout>